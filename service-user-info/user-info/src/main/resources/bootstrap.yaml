spring:
  application:
    name: service-user-info
  profiles:
    active: @springProfilesActive@
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
      discovery:
        server-addr: 127.0.0.1:8848
feign:
  sentinel:
    enabled: true